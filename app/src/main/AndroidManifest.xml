<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".fragments.perfil" >
        </activity>
        <activity
            android:name=".Final"
            android:label="@string/title_activity_final" />
        <activity
            android:name=".Resultados"
            android:label="@string/title_activity_resultados" />
        <activity
            android:name=".fragments.Perfil"
            android:label="@string/title_activity_perfil" />
        <activity
            android:name=".fragments.Resultados"
            android:label="@string/title_activity_resultados" />
        <activity android:name=".Resultado" />
        <activity android:name=".Perguntas" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>